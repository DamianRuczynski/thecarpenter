import { SafeResouorceUrlPipe } from './safe-resouorce-url.pipe';

describe('SafeResouorceUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeResouorceUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
